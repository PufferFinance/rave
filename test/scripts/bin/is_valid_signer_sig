#!/bin/bash

# stdin = signer cert, arg 1 = sig

UUID=$(cat /proc/sys/kernel/random/uuid)
echo $UUID
exit
pub_key_hex=$(openssl x509 -pubkey -noout | ./to_hex)
echo "$pub_key_hex" | ./from_hex | openssl dgst -signature sigfile datafile